package main

/*
import (
	"bufio"
	"fmt"
	"math"
	"os"
	"strconv"
)

var sc = bufio.NewScanner(os.Stdin)

func next() string {
	sc.Scan()
	return sc.Text()
}
func nextInt() int {
	i, _ := strconv.Atoi(next())
	return i
}
func nextFloat() float64 {
	i, _ := strconv.ParseFloat(next(), 64)
	return i
}

func isInteger(x float64) bool {
	return math.Floor(x) == x
}

var (
	slice []int
	c     []int
)

func main() {
	sc.Split(bufio.ScanWords)
	s1 := next()
	s2 := next()
	s3 := next()
	s4 := next()

	slice := [4]string{s1, s2, s3, s4}

	set := make(map[string]struct{})
	for _, v := range slice {
		set[v] = struct{}{}
	}

	if len(set) == 4 {
		fmt.Println("Yes")
	} else {
		fmt.Println("No")
	}
}
*/
